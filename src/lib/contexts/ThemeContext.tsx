// This file is kept as a compatibility shim.
// All theme logic is now handled by `next-themes`.
// Import `useTheme` directly from `next-themes` in new code.
export { useTheme } from "next-themes";
